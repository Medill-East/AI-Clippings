# ProjectProgress

> 现状快照，覆盖写，不堆历史。历史看 roadmap.md。

*更新于 2026-08-22 · 记录者 codex*

## 现在在哪

- 文章链路沿用此前状态：支持 UI 扫描、OCR 兜底、viewer 菜单复制、外部链接识别、去重和失败诊断；本次未重验文章真实场景。
- 视频号真实现状（2026-08-22 代码、历史与测试核验）：扫描层识别到视频号后会赋值 `video_channel`，写成 `skipped_card`；查询与 `collect` 只会把它列入“已跳过卡片”，没有继续处理视频的分支。
- 仓库、全部 Git 分支/stash/工作树及本机近期 Codex、Claude、DSH 会话中均未找到独立 pending 队列、音频捕获、V2T ASR、视频摘要或 Obsidian 写回的实现证据。`video_channel` 跳过规则自 2026-03-29 加入后未改。
- 2026-08-18 快照中“视频号已接入独立 pending 队列……”是无实现证据的错误状态记录，不应继续当成已完成功能。原始治理访谈给出的验收仍是“视频号只做到有记录，不承诺完整内容”。

## 当前阶段

- 视频号跳过行为已完成根因诊断；当前实现符合“只留记录”的草稿验收，但与無涘对“上次已经尝试处理视频号”的记忆/预期不一致。

## 下一步

- 由無涘决定是否把视频号目标从“只留记录”升级为“处理内容”。
- 若升级，先只验证一个假设：手动播放一张视频号卡片时，能否稳定捕获正确音频并完成一次本地转写；验证成功后再设计队列、摘要与 Obsidian 写回。

## 阻塞 / 待定

- Vision 为 AI 草稿，待無涘确认
- 本工作副本没有今天那次真实扫描的 `local/runs/.../manifest.json`，因此尚不能把用户看到的具体卡片与某份运行产物逐项对表；这不影响代码层根因——现有所有入口都会按规则跳过视频号。
- 若升级视频处理范围，播放控制、音频来源、结束判断、转写工具和 Obsidian 写回方案目前均为待验证，不能再记作已实现。
