# ProjectProgress

> 现状快照，覆盖写，不堆历史。历史看 `ProjectInfo/sessions/` 与 `ProjectInfo/dialogues/`。

*更新于 2026-08-22 · 记录者 Codex*

## 现在在哪

- 2026-08-22 的微信 FileHelper 扫描结果已完成 Obsidian Web Clipper 处理。
- “已收集链接”实际 31 条，均已生成新 note；首轮并发 10 成功 30 条，1 条在并发 2 重试后成功。
- 最终验收确认 31 个新文件均在 `/Users/haodong/Documents/GitHub/PKM/PlayWithExperiences/Clippings`，符合当前 Auto Mover 行为。
- 30 条 source URL 与输入完全一致；小黑盒外链被站点规范化为 `/app/bbs/link/9d5b7253e7c6` canonical URL，note 内容、标题和 `link_id` 均对应原始链接。
- 视频号真实现状（2026-08-22 代码、历史与测试核验）：扫描层识别到视频号后会赋值 `video_channel`，写成 `skipped_card`；查询与 `collect` 只会把它列入“已跳过卡片”，没有继续处理视频的分支。
- 仓库、Git 状态和近期会话中没有独立 pending 队列、音频捕获、V2T ASR、视频摘要或 Obsidian 写回的实现证据；此前“已接入视频处理”的快照属于错误状态记录。
- 视频号技术路线审计已覆盖本机微信/元宝功能、腾讯云正式 API、GitHub 主流下载器和下载后转写项目。普通视频不需要在微信前台完整播放；可在扫描阶段短时取得分享链接，再后台解析媒体、下载、转写和写回。
- 此前建议的 ScreenCaptureKit 前台实时录音已降为最后兜底。当前推荐主线是：元宝独立浏览器登录态调用腾讯域名解析链取得媒体 → 后台下载 → 复用本机 V2T 做完整转写 → 摘要与逐字稿写入 Obsidian。
- 微信 4.1.11 客户端确有“AI总结（由元宝提供）”，但本机功能须知写的是“文件或文章”；视频号评论区 `@元宝` 可总结已有腾讯高管公开发言支持，但完整逐字稿、私聊转发结果、稳定导出和自动调用接口仍未验证，不能先记作可用生产链。

## 当前阶段

- 文章剪藏链路本批次完成；视频号仍保持“只留记录”的当前实现。技术调查完成，处于确认最终产物契约与单卡验证设计阶段，尚未修改视频处理代码。

## 下一步

- 后续批次继续使用 `OBSIDIAN_CLIPPER_MAX_CONCURRENCY=10`。
- 最终验收继续按 vault 中的 source URL 和 Auto Mover 最终路径核对；对外链保留 canonical URL 映射检查。
- 先确认视频笔记是否必须保留完整逐字稿；按“完全的信息”当前建议默认保留“摘要 + 要点 + 完整逐字稿”。
- 微信恢复登录后，用一张普通视频号卡片分别验证：私聊转发给元宝的产物完整性，以及独立元宝浏览器登录态的后台分享链接解析。一次只验证一个假设。
- 单卡验证通过并获设计批准后，再实现“前台短时收集任务 → 后台解析/下载/V2T/写回”的队列；任何降级不得静默占用前台播放完整视频。

## 阻塞 / 待定

- Vision 为 AI 草稿，待無涘确认。
- 本批次待确认的截断链接 `https://www.zhihu.com/question` 未自动处理；14 个跳过卡片也未纳入文章剪藏。
- 刚才微信弹出“因账号安全需重新登录”；Computer Use 能控制窗口与键盘，但微信自绘/嵌入式正文几乎不暴露可访问性控件。现有证据不能确认自动化是退出原因，也不能排除相关性；恢复登录前不再做微信 UI 实测。
- 元宝分享链接解析使用未公开的腾讯内部 Web 接口，可能随服务变更；现成 macOS 下载器 `ltaoo/wx_channels_download` 需要本地代理/根证书，且近期有微信 4.1.12 兼容问题，只能作为明确授权的后备。
- 腾讯云 MPS 可正式异步执行 ASR 和大模型视频摘要，但需要云账号、凭证、计费和媒体可访问性；是否采用待产品契约与隐私/成本取舍确认。
