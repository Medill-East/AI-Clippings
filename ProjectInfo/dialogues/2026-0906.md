# 2026-09-06 对话记录

*更新于 2026-09-06 15:41 · 记录者 Codex*

## 1541 视频号重复短链与长视频中断

### 用户

> 处理视频号好像失败了，而且虽然说是3条，其实是被重复计算的同一条

用户附上的运行证据显示：扫描列出 3 个 `/sph/` 分享短链；后台停在 `[video 1/3] ASR 50/172`，随后 shell 报 `killed`。

### Codex

- 回读扫描 manifest、视频任务状态、失败日志和本机 V2T 实现；确认旧视频 manifest 停在 `running`，任务停在 `transcribing`，未产生 PKM 笔记。
- 对另外两个短链做了无重试、无下载、无付费模型调用的最小解析探针。三个短链的标题、作者、创建时间、媒体类型和内容指纹完全一致，证实它们是同一个视频。
- 以 TDD 修复批处理顺序：先解析全批身份并按内容指纹分组，再启动唯一视频任务；终端会明确显示“3 个分享短链 → 1 个唯一视频”，ASR 进度改为 `[video 1/1]`。
- 将本机 ASR 接入 V2T 已有的可恢复分片执行器：每个 20 秒分片在独立 worker 中运行并逐片留痕，避免原生识别器内存生命周期拖死批次。
- 增加任务状态脱敏，后续不再把带临时签名参数的封面 URL 写入 `task.json`。
- 验证：完整测试 `190 pass / 0 fail`；真实 3 秒音频探针完成 `1/1` worker 分片并产出 15 字转写。
- 代码提交 `a38176a`、`649641d` 已推送；实现真源和 GitHub 运行副本均 fast-forward 到 `649641d`。
- 将本次旧本地 manifest 改为 `interrupted`，canonical 任务改为 `failed / process_sigkill_observed`，另外两条别名标为未尝试；保留媒体和 WAV 供后续重试。

### 未执行

- 未重跑完整 50 分 54 秒视频；完整流程末段会调用 Codex 摘要，需要另行确认后再消耗该次外部调用与配额。

## 1543 微信文章批量剪藏

### 用户

> 确认处理

用户确认处理附件中的公众号文章链接；另说明如确有必要可以重新登录。

### Codex

- 从附件提取并校验 22 条唯一 `mp.weixin.qq.com/s/` URL；视频号、图片 OCR 和 unresolved 项按类型分流，没有交给文章剪藏。
- 首次启动暴露 Playwright 缓存路径失效；随后确认 Google Chrome 152 忽略命令行扩展加载参数。本机已有 Chrome for Testing 151 缓存可用，切换本地剪藏配置后扩展 worker、headed 启动和 `chrome.storage` 均通过真实探针。
- 首轮并发 10 表面为 22/22 success，但逐条验收发现 5 条 active-tab 错配假成功。增加 URL-to-tab 映射回归测试和实现，用页面当前 URL 精确查询 tab ID；5 条受影响 URL 改为串行重跑。
- 最终文章验收为 22/22：每条 `clipTarget.sourceUrl` 与请求 URL 一致，22 个笔记路径互不重复，文件均存在、非空并含对应源 URL。5 个错配产物已从 vault 移至可恢复隔离区。
- 本轮 `npm test` 为 17 pass / 0 fail；文章运行 manifest 为 `obsidian-web-clipper-ingest/local/runs/2026-09-06T07-26-52-414Z/manifest.json`，修复重跑 manifest 为 `local/runs/2026-09-06T07-36-22-423Z/manifest.json`。

原始对话：本文件「1543 微信文章批量剪藏」；主题摘要：`ProjectInfo/sessions/2026-0906-obsidian-web-clipper-batch.md`
