# 2026-08-22 对话记录

## 1312 视频号仍被跳过诊断

用户：

> 记得上次我们不是说尝试了能够处理那个微信的视频号的内容吗？今天我看它好像还是跳过了，你看一下呢。

Codex：

> 查清了：今天视频号仍会被跳过，而且这不是一次近期回归。现有代码从 2026-03-29 起就把视频号识别为 `video_channel`，记录成 `skipped_card`；`collect` 只展示“已跳过卡片”，没有后续视频处理分支。
>
> 2026-08-18 的 ProjectProgress 曾写“已接入 pending 队列、音频捕获、V2T ASR、摘要和 Obsidian 写回”，但仓库、Git 历史与本机近期会话中都没有这些实现证据；原始治理访谈的验收其实是“视频号只做到有记录，不承诺完整内容”。因此根因是项目状态被误记成“已实现”，不是今天的链路突然失效。
>
> 本次只完成诊断与状态纠正，没有修改采集逻辑。若要真正处理内容，下一步应先用一张视频号卡片验证“手动播放 → 正确音频捕获 → 一次本地转写”这一个最小假设，再决定是否扩展完整链路。

产出：`ProjectInfo/ProjectProgress.md`、`ProjectInfo/sessions/2026-0822-wechat-video-channel-skip-diagnosis.md`

## 1315 Obsidian Web Clipper 批处理

用户：提供了微信 FileHelper macOS ingest 的 2026-08-22 12:00-23:59 扫描输出，并要求“处理”。

Codex：读取项目进展、项目愿景和 Obsidian Web Clipper skill。根据扫描报告区分：31 条“已收集链接”、1 条待确认的截断知乎前缀、14 个跳过卡片。决定只处理 31 条已收集链接。

Codex：创建并校验输入文件，确认输入与附件逐条一致：31 条、无缺失、无多余、无重复。运行 `node ./scripts/setup.js`，Chrome、隔离 profile 和扩展 `1.7.1_0` 预检通过。

Codex：执行 `OBSIDIAN_CLIPPER_MAX_CONCURRENCY=10 node ./scripts/clip-links.js --input local/inputs/2026-08-22-filehelper-links.txt`。首轮并发 10 为 30 成功、1 失败；失败项由 skill 的并发 2 重试恢复。最终输出 `Success: 31 Failed: 0 Dry-run: 0`。

Codex：不以 manifest 单独作为验收依据，扫描目标 vault 的实际 Markdown。确认 31 个新文件都写入 Auto Mover 的最终目录 `Clippings`。其中 30 个 source 精确匹配；小黑盒外链在页面中从 `/bbs/post_share` 规范化为 `/app/bbs/link/9d5b7253e7c6`，note 的 canonical source、标题、正文摘要与原始 `link_id` 对应，因此记录为已成功但保留 canonical 差异。

产出：

- `obsidian-web-clipper-ingest/local/inputs/2026-08-22-filehelper-links.txt`
- `obsidian-web-clipper-ingest/local/runs/2026-08-22T05-07-00-690Z/manifest.json`
- `/Users/haodong/Documents/GitHub/PKM/PlayWithExperiences/Clippings` 下 31 个本次新建 Markdown

决策：無涘 ｜ 记录：Codex
